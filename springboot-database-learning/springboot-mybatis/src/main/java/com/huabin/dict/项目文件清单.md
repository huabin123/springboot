# 字典管理模块 - 项目文件清单

## 创建时间
2026-03-09

## 文件列表

### 1. 实体类（Entity）
- **路径**: `com.huabin.dict.entity`
- **文件**:
  - `SysDictType.java` - 字典类型实体类
  - `SysDictItem.java` - 字典项实体类

### 2. 视图对象（VO）
- **路径**: `com.huabin.dict.vo`
- **文件**:
  - `SysDictTypeVO.java` - 字典类型视图对象，包含字典项列表

### 3. 数据访问层（Mapper）
- **路径**: `com.huabin.dict.mapper`
- **文件**:
  - `SysDictTypeMapper.java` - Mapper接口
  - `resources/mapper/SysDictTypeMapper.xml` - MyBatis XML映射文件

### 4. 业务逻辑层（Service）
- **路径**: `com.huabin.dict.service`
- **文件**:
  - `SysDictTypeService.java` - 服务接口
  - `impl/SysDictTypeServiceImpl.java` - 服务实现类

### 5. 控制器层（Controller）
- **路径**: `com.huabin.dict.controller`
- **文件**:
  - `SysDictTypeController.java` - REST控制器

### 6. 测试类（Test）
- **路径**: `src/test/java/com/huabin/dict`
- **文件**:
  - `SysDictTypeMapperTest.java` - Mapper测试类

### 7. 文档和脚本
- **路径**: `com.huabin.dict`
- **文件**:
  - `README.md` - 详细的需求文档和使用说明
  - `init.sql` - 数据库初始化脚本（建表+测试数据）
  - `项目文件清单.md` - 本文件

### 8. 配置文件修改
- **文件**: `src/main/resources/application.yml`
- **修改内容**:
  - 添加了 `com.huabin.dict.entity` 和 `com.huabin.dict.vo` 到 `type-aliases-package`
  - 添加了 `com.huabin.dict.mapper` 到日志配置

## 目录结构

```
src/main/java/com/huabin/dict/
├── controller/
│   └── SysDictTypeController.java
├── entity/
│   ├── SysDictType.java
│   └── SysDictItem.java
├── mapper/
│   └── SysDictTypeMapper.java
├── service/
│   ├── SysDictTypeService.java
│   └── impl/
│       └── SysDictTypeServiceImpl.java
├── vo/
│   └── SysDictTypeVO.java
├── README.md
├── init.sql
└── 项目文件清单.md

src/main/resources/mapper/
└── SysDictTypeMapper.xml

src/test/java/com/huabin/dict/
└── SysDictTypeMapperTest.java
```

## 快速开始

### 1. 初始化数据库
```bash
# 连接到MySQL数据库
mysql -u root -p

# 选择数据库
use springboot_db;

# 执行初始化脚本
source /path/to/init.sql
```

或者直接在MySQL客户端中执行 `init.sql` 文件的内容。

### 2. 启动应用
确保Spring Boot应用已启动。

### 3. 测试接口
```bash
# 查询顶级字典（dictPid = 0）
curl "http://localhost:8080/dict/types?dictPid=0"

# 查询子字典（dictPid = 1）
curl "http://localhost:8080/dict/types?dictPid=1"
```

### 4. 运行单元测试
```bash
# 在项目根目录执行
mvn test -Dtest=SysDictTypeMapperTest
```

## 核心技术点

### MyBatis Collection映射
使用MyBatis的 `<collection>` 标签实现一对多关系映射：
```xml
<resultMap id="SysDictTypeVOResult" type="com.huabin.dict.vo.SysDictTypeVO">
    <id property="dictId" column="dict_id"/>
    <result property="dictName" column="dict_name"/>
    <collection property="items" ofType="com.huabin.dict.entity.SysDictItem" resultMap="SysDictItemResult"/>
</resultMap>
```

### 单SQL查询
通过 LEFT JOIN 实现单SQL查询，避免N+1问题：
```sql
SELECT dt.dict_id, dt.dict_name, di.item_id, di.dict_id, di.item_name, di.item_value
FROM sys_dict_type dt
LEFT JOIN sys_dict_item di ON dt.dict_id = di.dict_id
WHERE dt.dict_pid = #{dictPid}
ORDER BY dt.dict_id, di.item_id
```

## 注意事项

1. **数据库连接**: 确保 `application.yml` 中的数据库连接配置正确
2. **表结构**: 执行 `init.sql` 前确认表不存在或备份现有数据
3. **包扫描**: 已配置 `@Mapper` 注解，Spring Boot会自动扫描
4. **日志级别**: 已配置DEBUG级别，可以看到完整的SQL执行日志

## 扩展建议

- 添加字典缓存（Redis）
- 实现字典的增删改功能
- 添加字典排序字段
- 实现字典树的递归查询
- 添加数据权限控制
