# 02-代码生成器使用指南

## 📌 MyBatis Generator 简介

MyBatis Generator（MBG）是 MyBatis 官方提供的代码生成工具，可以根据数据库表自动生成：
- 实体类（Entity/Model）
- Mapper 接口
- Mapper XML 文件

---

## 🚀 快速开始

### 1. 准备数据库表

在使用代码生成器之前，需要先创建数据库表。

**主数据源（springboot_db）示例表：**

```sql
CREATE DATABASE IF NOT EXISTS springboot_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE springboot_db;

CREATE TABLE `user` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `username` VARCHAR(50) NOT NULL COMMENT '用户名',
  `password` VARCHAR(100) NOT NULL COMMENT '密码',
  `email` VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `status` INT(11) DEFAULT '1' COMMENT '状态：0-禁用，1-启用',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

**从数据源（springboot_db2）示例表：**

```sql
CREATE DATABASE IF NOT EXISTS springboot_db2 CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE springboot_db2;

CREATE TABLE `product` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '产品ID',
  `product_name` VARCHAR(100) NOT NULL COMMENT '产品名称',
  `product_code` VARCHAR(50) NOT NULL COMMENT '产品编码',
  `price` DECIMAL(10,2) NOT NULL COMMENT '产品价格',
  `stock` INT(11) DEFAULT '0' COMMENT '库存数量',
  `description` VARCHAR(500) DEFAULT NULL COMMENT '产品描述',
  `status` INT(11) DEFAULT '1' COMMENT '状态：0-下架，1-上架',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_product_code` (`product_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='产品表';
```

---

### 2. 配置生成器文件

项目已提供两个生成器配置文件：

- `src/main/resources/generator/generatorConfig-db1.xml` - 主数据源
- `src/main/resources/generator/generatorConfig-db2.xml` - 从数据源

**修改配置文件中的表名：**

```xml
<!-- 修改要生成的表 -->
<table tableName="user" domainObjectName="User" 
       enableCountByExample="false" 
       enableUpdateByExample="false"
       enableDeleteByExample="false" 
       enableSelectByExample="false"
       selectByExampleQueryId="false">
    <generatedKey column="id" sqlStatement="JDBC"/>
</table>

<!-- 可以配置多个表 -->
<table tableName="order" domainObjectName="Order" 
       enableCountByExample="false" 
       enableUpdateByExample="false"
       enableDeleteByExample="false" 
       enableSelectByExample="false"
       selectByExampleQueryId="false">
    <generatedKey column="id" sqlStatement="JDBC"/>
</table>
```

---

### 3. 运行代码生成器

#### 方式一：使用 Maven 命令（推荐）

**生成主数据源代码：**
```bash
mvn mybatis-generator:generate -Dmybatis.generator.configurationFile=src/main/resources/generator/generatorConfig-db1.xml
```

**生成从数据源代码：**
```bash
mvn mybatis-generator:generate -Dmybatis.generator.configurationFile=src/main/resources/generator/generatorConfig-db2.xml
```

#### 方式二：使用 IDEA 插件

1. 安装 MyBatis Generator 插件
2. 右键点击 `generatorConfig-db1.xml` 或 `generatorConfig-db2.xml`
3. 选择 "Run MyBatis Generator"

#### 方式三：使用 Java 代码

```java
public class GeneratorMain {
    public static void main(String[] args) throws Exception {
        List<String> warnings = new ArrayList<>();
        boolean overwrite = true;
        
        // 指定配置文件
        File configFile = new File("src/main/resources/generator/generatorConfig-db1.xml");
        ConfigurationParser cp = new ConfigurationParser(warnings);
        Configuration config = cp.parseConfiguration(configFile);
        
        DefaultShellCallback callback = new DefaultShellCallback(overwrite);
        MyBatisGenerator myBatisGenerator = new MyBatisGenerator(config, callback, warnings);
        myBatisGenerator.generate(null);
        
        // 打印警告信息
        for (String warning : warnings) {
            System.out.println(warning);
        }
    }
}
```

---

## 📁 生成文件位置

### 主数据源（springboot_db）

| 文件类型 | 生成位置 | 示例 |
|---------|---------|------|
| 实体类 | `src/main/java/com/huabin/multids/db1/entity/` | User.java |
| Mapper接口 | `src/main/java/com/huabin/multids/db1/mapper/` | UserMapper.java |
| Mapper XML | `src/main/resources/mapper/db1/` | UserMapper.xml |

### 从数据源（springboot_db2）

| 文件类型 | 生成位置 | 示例 |
|---------|---------|------|
| 实体类 | `src/main/java/com/huabin/multids/db2/entity/` | Product.java |
| Mapper接口 | `src/main/java/com/huabin/multids/db2/mapper/` | ProductMapper.java |
| Mapper XML | `src/main/resources/mapper/db2/` | ProductMapper.xml |

---

## ⚙️ 配置文件详解

### 基本配置

```xml
<generatorConfiguration>
    <context id="DB1Tables" targetRuntime="MyBatis3">
        
        <!-- 数据库连接配置 -->
        <jdbcConnection 
            driverClass="com.mysql.cj.jdbc.Driver"
            connectionURL="jdbc:mysql://localhost:3306/springboot_db?..."
            userId="springboot"
            password="Huabin123$">
            <!-- MySQL 8.x 必须配置 -->
            <property name="nullCatalogMeansCurrent" value="true"/>
        </jdbcConnection>
        
        <!-- 其他配置... -->
        
    </context>
</generatorConfiguration>
```

### 注释配置

```xml
<commentGenerator>
    <!-- 是否去除自动生成的注释 -->
    <property name="suppressAllComments" value="false"/>
    <!-- 是否生成注释时间戳 -->
    <property name="suppressDate" value="true"/>
    <!-- 是否添加数据库表的备注信息 -->
    <property name="addRemarkComments" value="true"/>
</commentGenerator>
```

### Java 类型解析器

```xml
<javaTypeResolver>
    <!-- 是否使用BigDecimal，false则使用Integer/Long等 -->
    <property name="forceBigDecimals" value="false"/>
    <!-- 是否使用JSR310日期类型（LocalDateTime等） -->
    <property name="useJSR310Types" value="true"/>
</javaTypeResolver>
```

**类型映射：**

| 数据库类型 | useJSR310Types=true | useJSR310Types=false |
|-----------|---------------------|----------------------|
| DATE | LocalDate | Date |
| TIME | LocalTime | Date |
| DATETIME | LocalDateTime | Date |
| TIMESTAMP | LocalDateTime | Date |

### 实体类配置

```xml
<javaModelGenerator 
    targetPackage="com.huabin.multids.db1.entity" 
    targetProject="src/main/java">
    <!-- 是否在当前路径下新建一层schema子包 -->
    <property name="enableSubPackages" value="false"/>
    <!-- 是否针对String类型的字段在set方法中进行trim操作 -->
    <property name="trimStrings" value="true"/>
</javaModelGenerator>
```

### Mapper XML 配置

```xml
<sqlMapGenerator 
    targetPackage="mapper.db1" 
    targetProject="src/main/resources">
    <property name="enableSubPackages" value="false"/>
</sqlMapGenerator>
```

### Mapper 接口配置

```xml
<javaClientGenerator 
    type="XMLMAPPER" 
    targetPackage="com.huabin.multids.db1.mapper" 
    targetProject="src/main/java">
    <property name="enableSubPackages" value="false"/>
</javaClientGenerator>
```

**type 参数说明：**

- `XMLMAPPER`：生成基于 XML 的 Mapper（推荐）
- `ANNOTATEDMAPPER`：生成基于注解的 Mapper
- `MIXEDMAPPER`：混合模式

### 表配置

```xml
<table tableName="user" domainObjectName="User" 
       enableCountByExample="false" 
       enableUpdateByExample="false"
       enableDeleteByExample="false" 
       enableSelectByExample="false"
       selectByExampleQueryId="false">
    
    <!-- 主键生成策略 -->
    <generatedKey column="id" sqlStatement="JDBC"/>
    
    <!-- 忽略某些列 -->
    <ignoreColumn column="deleted"/>
    
    <!-- 列重命名 -->
    <columnOverride column="user_name" property="username"/>
    
    <!-- 指定列的Java类型 -->
    <columnOverride column="status" javaType="java.lang.Integer"/>
</table>
```

**table 参数说明：**

| 参数 | 说明 | 推荐值 |
|------|------|--------|
| tableName | 数据库表名 | 必填 |
| domainObjectName | 生成的实体类名 | 建议填写 |
| enableCountByExample | 生成 countByExample 方法 | false |
| enableUpdateByExample | 生成 updateByExample 方法 | false |
| enableDeleteByExample | 生成 deleteByExample 方法 | false |
| enableSelectByExample | 生成 selectByExample 方法 | false |

**为什么推荐设置为 false？**
- Example 方法生成大量冗余代码
- 实际项目中很少使用
- 建议根据需求手动编写查询方法

---

## 🎯 生成策略

### 1. 生成指定表

```xml
<!-- 生成单个表 -->
<table tableName="user" domainObjectName="User" .../>

<!-- 生成多个表 -->
<table tableName="user" domainObjectName="User" .../>
<table tableName="order" domainObjectName="Order" .../>
<table tableName="product" domainObjectName="Product" .../>
```

### 2. 生成所有表（不推荐）

```xml
<!-- 使用通配符生成所有表 -->
<table tableName="%" .../>
```

**不推荐原因：**
- 可能生成不需要的表
- 无法控制实体类名称
- 可能覆盖已有的自定义代码

### 3. 使用表名模式

```xml
<!-- 生成所有以 t_ 开头的表 -->
<table tableName="t_%" .../>

<!-- 生成所有以 _info 结尾的表 -->
<table tableName="%_info" .../>
```

---

## 🔧 高级配置

### 1. 自定义注释

创建自定义注释生成器：

```java
public class MyCommentGenerator extends DefaultCommentGenerator {
    @Override
    public void addFieldComment(Field field, IntrospectedTable introspectedTable,
                                IntrospectedColumn introspectedColumn) {
        // 自定义字段注释
        field.addJavaDocLine("/**");
        field.addJavaDocLine(" * " + introspectedColumn.getRemarks());
        field.addJavaDocLine(" */");
    }
}
```

在配置文件中使用：

```xml
<commentGenerator type="com.huabin.generator.MyCommentGenerator">
    <property name="suppressAllComments" value="false"/>
</commentGenerator>
```

### 2. 自定义插件

```xml
<plugin type="org.mybatis.generator.plugins.SerializablePlugin"/>
<plugin type="org.mybatis.generator.plugins.ToStringPlugin"/>
<plugin type="org.mybatis.generator.plugins.EqualsHashCodePlugin"/>
```

**常用插件：**

- `SerializablePlugin`：实体类实现 Serializable 接口
- `ToStringPlugin`：生成 toString 方法
- `EqualsHashCodePlugin`：生成 equals 和 hashCode 方法
- `MapperAnnotationPlugin`：在 Mapper 接口上添加 @Mapper 注解

### 3. 自定义类型处理器

```xml
<javaTypeResolver>
    <property name="forceBigDecimals" value="false"/>
    <property name="useJSR310Types" value="true"/>
</javaTypeResolver>

<!-- 自定义类型映射 -->
<table tableName="user">
    <columnOverride column="status" 
                    javaType="com.huabin.enums.UserStatus"
                    typeHandler="com.huabin.handler.UserStatusTypeHandler"/>
</table>
```

---

## 📝 使用示例

### 完整流程示例

**1. 创建数据库表**

```sql
CREATE TABLE `user` (
  `id` BIGINT(20) NOT NULL AUTO_INCREMENT,
  `username` VARCHAR(50) NOT NULL,
  `email` VARCHAR(100),
  `status` INT(11) DEFAULT '1',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**2. 配置 generatorConfig-db1.xml**

```xml
<table tableName="user" domainObjectName="User" 
       enableCountByExample="false" 
       enableUpdateByExample="false"
       enableDeleteByExample="false" 
       enableSelectByExample="false">
    <generatedKey column="id" sqlStatement="JDBC"/>
</table>
```

**3. 运行生成命令**

```bash
mvn mybatis-generator:generate -Dmybatis.generator.configurationFile=src/main/resources/generator/generatorConfig-db1.xml
```

**4. 生成的文件**

```
src/main/java/com/huabin/multids/db1/
├── entity/
│   └── User.java
└── mapper/
    └── UserMapper.java

src/main/resources/mapper/db1/
└── UserMapper.xml
```

**5. 使用生成的代码**

```java
@Service
public class UserService {
    @Autowired
    private UserMapper userMapper;
    
    public User getUserById(Long id) {
        return userMapper.selectByPrimaryKey(id);
    }
}
```

---

## ⚠️ 注意事项

### 1. 覆盖问题

**问题：** 重新生成代码会覆盖已有文件

**解决方案：**

- ✅ 实体类和 Mapper 接口：可以覆盖（通常不修改）
- ❌ Mapper XML：不要覆盖（可能包含自定义 SQL）

**建议：**
- 首次生成后，将 XML 文件备份
- 或者在 XML 文件中添加自定义 SQL，不要修改生成的部分

### 2. 字段映射

**问题：** 数据库字段名与 Java 属性名不一致

**解决方案：**

```xml
<!-- 方式1：使用 columnOverride -->
<table tableName="user">
    <columnOverride column="user_name" property="username"/>
</table>

<!-- 方式2：开启驼峰命名转换（推荐） -->
<!-- 在 MyBatis 配置中设置 -->
<configuration>
    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>
</configuration>
```

### 3. 日期类型

**问题：** 生成的日期类型不是 LocalDateTime

**解决方案：**

```xml
<javaTypeResolver>
    <!-- 使用 JSR310 日期类型 -->
    <property name="useJSR310Types" value="true"/>
</javaTypeResolver>
```

### 4. MySQL 8.x 兼容性

**问题：** 生成时找不到表或生成了其他数据库的表

**解决方案：**

```xml
<jdbcConnection ...>
    <!-- 必须添加此配置 -->
    <property name="nullCatalogMeansCurrent" value="true"/>
</jdbcConnection>
```

---

## 🔍 常见问题

### Q1: 如何只生成部分方法？

**A:** 在 table 标签中配置：

```xml
<table tableName="user" domainObjectName="User" 
       enableCountByExample="false"     <!-- 不生成 count 方法 -->
       enableUpdateByExample="false"    <!-- 不生成 update example 方法 -->
       enableDeleteByExample="false"    <!-- 不生成 delete example 方法 -->
       enableSelectByExample="false">   <!-- 不生成 select example 方法 -->
</table>
```

### Q2: 如何自定义生成的方法名？

**A:** MyBatis Generator 不支持直接自定义方法名，建议：
- 使用生成的基础方法
- 在 Mapper 接口中添加自定义方法
- 在 XML 文件中添加自定义 SQL

### Q3: 如何生成 Lombok 注解？

**A:** 需要自定义插件或使用第三方插件：

```xml
<plugin type="com.softwareloop.mybatis.generator.plugins.LombokPlugin">
    <property name="builder" value="true"/>
    <property name="allArgsConstructor" value="true"/>
</plugin>
```

### Q4: 生成的代码如何添加到版本控制？

**A:** 建议：
- 实体类和 Mapper 接口：加入版本控制
- Mapper XML：加入版本控制，但标记为生成的部分不要修改
- 自定义的 SQL 单独管理

---

## 📚 相关文档

- [01-多数据源配置说明.md](./01-多数据源配置说明.md)
- [03-使用示例.md](./03-使用示例.md)
- [README.md](./README.md)
- [MyBatis Generator 官方文档](http://mybatis.org/generator/)
