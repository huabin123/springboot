server:
  port: 8082

spring:
  application:
    name: consumer-service

# Feign 配置
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full

logging:
  level:
    com.huabin: DEBUG
    com.huabin.consumer.feign: DEBUG
